model{

    #The likelihood:

    for (i in 1:length(x)){
        S[i] <- (A * exp((-(x[i]-x0)^2) / ( 2 *w^2)) + B)

        y[i] ~ dpois(S[i]) 
    } 


    # Priors for  A, B, x0, w

    A ~ dunif( 152 , 7626 )
    B ~ dunif( 0 , 228 )

    x0 ~ dunif( 529 , 559 )
    w ~ dunif(1,10)
      
}