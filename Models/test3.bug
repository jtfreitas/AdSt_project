model{

    #The likelihood:

    for (i in 1:length(x)){
        S[i] <- (A * exp((-(x[i]-x0)^2) / ( 2 *w^2)) + B)

        y[i] ~ dpois(S[i]) 
    } 


    # Priors for  A, B, x0, w

    A ~ dunif( 116 , 2703 )
    B ~ dunif( 0 , 174 )

    x0 ~ dunif( 899 , 999 )
    w ~ dunif(1,10)
      
}